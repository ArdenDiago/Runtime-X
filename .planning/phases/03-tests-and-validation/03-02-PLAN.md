---
phase: 03-tests-and-validation
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified: []
autonomous: false
requirements:
  - TEST-06

must_haves:
  truths:
    - "`rtx run yes` outputs lines in real-time (not buffered until exit)"
    - "Human has visually confirmed real-time streaming behavior"
  artifacts: []
  key_links: []
---

<objective>
Run manual validation for TEST-06 (real-time streaming) and execute the full test suite one final time to confirm Phase 3 is complete.

Purpose: TEST-06 cannot be automated in `go test` without a PTY. Human must visually confirm that `rtx run yes` streams output line-by-line in real time.
Output: Human-verified confirmation that all Phase 3 requirements pass.
</objective>

<execution_context>
@/home/ardendiago/.claude/get-shit-done/workflows/execute-plan.md
@/home/ardendiago/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-tests-and-validation/03-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebuild binary and run full test suite</name>
  <files></files>
  <action>
1. Rebuild the binary to ensure latest code: `go build -o bin/rtx ./cmd/rtx/`
2. Run full automated test suite: `go test -race -v ./internal/process/...`
3. Run vet: `go vet ./internal/process/...`
4. Confirm all TEST-01 through TEST-05 pass before proceeding to manual check.

If any test fails, investigate and fix before proceeding to the checkpoint.
  </action>
  <verify>
`go test -race -v ./internal/process/...` passes all tests.
`go vet ./internal/process/...` clean.
`bin/rtx` binary exists and is freshly built.
  </verify>
  <done>All automated tests pass, binary is rebuilt, ready for manual validation.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Manual validation — real-time streaming (TEST-06)</name>
  <what-built>Automated tests for TEST-01 through TEST-05 pass. Binary rebuilt. Ready for manual streaming check.</what-built>
  <how-to-verify>
Run this command in your terminal:

```bash
timeout 2 ./bin/rtx run yes 2>/dev/null | head -5
```

**Expected behavior:**
1. Five lines of `y` appear quickly (sub-second), not all at once after timeout
2. Output streams line-by-line in real time

**Alternative check (more visual):**
```bash
./bin/rtx run yes 2>/dev/null
```
Then press Ctrl+C after observing streaming output. Lines of `y` should scroll rapidly and continuously — not appear in a single burst.

**What to look for:**
- PASS: Lines appear immediately and continuously as they are produced
- FAIL: No output appears until the process exits, then all lines appear at once
  </how-to-verify>
  <resume-signal>Type "approved" if streaming works correctly, or describe any issues observed.</resume-signal>
</task>

</tasks>

<verification>
1. `go test -race -v ./internal/process/...` — all automated tests pass
2. Human confirms `rtx run yes` streams output in real time (TEST-06)
3. All six TEST requirements (TEST-01 through TEST-06) are satisfied
</verification>

<success_criteria>
All Phase 3 requirements verified: TEST-01 through TEST-05 via automated tests, TEST-06 via human observation. Phase 3 complete.
</success_criteria>

<output>
After completion, create `.planning/phases/03-tests-and-validation/03-02-SUMMARY.md`
</output>

---
phase: 02-signal-forwarding
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified: []
autonomous: false
requirements: [SIG-01, SIG-02, SIG-03, SIG-04, EXIT-03, ERR-03, LOG-02]

must_haves:
  truths:
    - "User presses Ctrl+C during rtx run sleep 100 and sees [rtx] received signal interrupt on stderr"
    - "rtx exits with code 130 after Ctrl+C (SIGINT, 128+2)"
    - "rtx exits with code 143 after SIGTERM (128+15)"
    - "rtx run sh -c 'exit 42' still exits with code 42 (natural exit unchanged)"
    - "No zombie processes after signal-terminated child"
  artifacts:
    - path: "bin/rtx"
      provides: "Signal-forwarding binary for behavioral verification"
  key_links:
    - from: "user Ctrl+C (SIGINT)"
      to: "stderr: [rtx] received signal interrupt"
      via: "signal.Notify -> sigCh -> LOG-02 log line"
      pattern: "received signal interrupt"
    - from: "SIGINT kill"
      to: "shell reports exit code 130"
      via: "resolveExitCode 128+N path"
      pattern: "echo \\$\\? -> 130"
---

<objective>
Rebuild the bin/rtx binary from the Phase 2 implementation and run behavioral verification across all four Phase 2 success criteria: signal logging, SIGINT exit code 130, SIGTERM exit code 143, and already-dead child graceful handling.

Purpose: Confirm the signal forwarding implementation is correct end-to-end — not just that it compiles, but that it behaves correctly under real OS signal delivery.

Output: Human-verified confirmation that all Phase 2 success criteria pass.
</objective>

<execution_context>
@/home/ardendiago/.claude/get-shit-done/workflows/execute-plan.md
@/home/ardendiago/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-signal-forwarding/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebuild binary and run automated signal verification</name>
  <files></files>
  <action>
Build the Phase 2 binary and run each behavioral check with explicit expected outputs. All commands assume the working directory is the project root.

Step 1 — Rebuild binary:
```
go build -o bin/rtx ./cmd/rtx
```
Expected: exits 0, no output.

Step 2 — Verify signal log appears (SIG-01, LOG-02):
```
bin/rtx run sleep 100 &
RTX_PID=$!
sleep 0.2
kill -SIGINT $RTX_PID
wait $RTX_PID
```
Expected stderr output: `[rtx] received signal interrupt`
Expected: rtx exits (does not hang)

Step 3 — Verify SIGINT exit code 130 (EXIT-03):
```
bin/rtx run sleep 100 &
RTX_PID=$!
sleep 0.2
kill -SIGINT $RTX_PID
wait $RTX_PID
echo "SIGINT exit code: $?"
```
Expected output: `SIGINT exit code: 130`

Step 4 — Verify SIGTERM exit code 143 (EXIT-03, SIG-02):
```
bin/rtx run sleep 100 &
RTX_PID=$!
sleep 0.2
kill -SIGTERM $RTX_PID
wait $RTX_PID
echo "SIGTERM exit code: $?"
```
Expected output: `SIGTERM exit code: 143`

Step 5 — Verify natural exit unchanged (regression check):
```
bin/rtx run sh -c 'exit 42'
echo "Natural exit code: $?"
```
Expected output: `Natural exit code: 42`

Step 6 — Verify no zombie after signal (PROC-04):
```
bin/rtx run sleep 1 &
RTX_PID=$!
sleep 0.2
kill -SIGINT $RTX_PID
sleep 0.5
ps aux | grep -E '\sZ\s' | grep -v grep
```
Expected: empty output (no zombie processes)

Step 7 — Verify command-not-found still works (regression check):
```
bin/rtx run nonexistent-command-xyz
echo "Not-found exit code: $?"
```
Expected output: `[rtx] command not found: nonexistent-command-xyz` on stderr, `Not-found exit code: 127` on stdout.

Record actual outputs from each step for the checkpoint below.
  </action>
  <verify>
go build -o bin/rtx ./cmd/rtx exits 0.
Steps 2-7 produce outputs matching expected values listed above.
  </verify>
  <done>
bin/rtx is rebuilt successfully.
All 6 behavioral checks produce the expected outputs.
Results are documented and ready for the human verification checkpoint.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of Phase 2 signal behavior</name>
  <action>Review automated check outputs from Task 1 and confirm all Phase 2 behavioral criteria pass.</action>
  <what-built>
Signal forwarding implementation in internal/process/runner.go. The binary intercepts SIGINT and SIGTERM, logs receipt, forwards to child, waits for child to exit, and emits POSIX exit codes (130 for SIGINT, 143 for SIGTERM). Task 1 above ran automated checks and recorded results.
  </what-built>
  <how-to-verify>
Review the automated check outputs from Task 1 and confirm each criterion:

1. Step 2 output — Does stderr contain `[rtx] received signal interrupt`? (SIG-01, LOG-02)
2. Step 3 output — Is the SIGINT exit code 130? (EXIT-03)
3. Step 4 output — Is the SIGTERM exit code 143? (EXIT-03, SIG-02)
4. Step 5 output — Is the natural exit code 42? (regression — Phase 1 behavior intact)
5. Step 6 output — Is the zombie check empty? (PROC-04 still intact)
6. Step 7 output — Is the command-not-found exit code 127? (regression — ERR-01 intact)

Optional manual check (interactive feel):
```
bin/rtx run sleep 100
```
Press Ctrl+C — you should see `[rtx] received signal interrupt` on stderr and the shell should report exit code 130.
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe any failures.</resume-signal>
</task>

</tasks>

<verification>
Phase 2 is complete when:
- bin/rtx runs with signal forwarding visible in all 4 ROADMAP success criteria:
  1. Ctrl+C shows "[rtx] received signal interrupt" before exit
  2. SIGTERM causes clean termination with child's code
  3. Ctrl+C produces exit code 130
  4. Signal to already-dead child does not crash rtx
- All Phase 1 behaviors still work (regression: exit 42, command-not-found 127, no zombies)
</verification>

<success_criteria>
- Human confirms all 6 automated check results match expected values
- Interactive Ctrl+C test produces "[rtx] received signal interrupt" on stderr
- Shell reports exit code 130 after Ctrl+C
- No Phase 1 regressions
</success_criteria>

<output>
After completion, create `.planning/phases/02-signal-forwarding/02-02-SUMMARY.md` following the summary template at @/home/ardendiago/.claude/get-shit-done/templates/summary.md
</output>
